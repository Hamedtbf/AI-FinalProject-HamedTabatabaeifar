# 🫁 Pneumonia Detection Framework: Custom CNN vs. Optimized DenseNet

این پروژه یک سیستم جامع برای تشخیص بیماری **پنومونی (سینه پهلو)** از روی تصاویر رادیولوژی است. در این پژوهش، ابتدا یک مدل CNN اختصاصی طراحی شد و سپس استراتژی‌های بهینه‌سازی روی معماری DenseNet برای رسیدن به بالاترین سطح کارایی و تفسیرپذیری مورد آزمایش قرار گرفت.

---

## 🏗️ فاز صفر: مدل اختصاصی (Custom CNN) - دقت ۸۵٪
پیش از استفاده از مدل‌های سنگین، یک شبکه عصبی کانولوشنال اختصاصی طراحی گردید:
* **ساختار:** استفاده از ۵ لایه کانولوشن با فیلترهای افزایشی و لایه‌های Dropout جهت کنترل ظرفیت مدل.
* **نتیجه:** این مدل موفق شد به دقت **۸۵٪** دست یابد. 
* **تحلیل:** سادگی معماری و تعداد بهینه پارامترها باعث شد مدل بدون درگیر شدن در پیچیدگی‌های معماری‌های غول‌آسا، الگوهای کلیدی بافت ریه را به خوبی شناسایی کند.



---

## 🔬 روند توسعه و بهینه‌سازی مدل DenseNet
در ادامه، برای بررسی پتانسیل مدل‌های عمیق‌تر، از معماری **DenseNet** در سه فاز مهندسی‌شده استفاده کردیم:

### فاز اول: Baseline Training (دقت ۷۰٪)
* **استراتژی:** اعمال نرمال‌سازی داده‌ها و رفع مشکل عدم توازن کلاس‌ها (Class Imbalance).
* **نتیجه:** علیرغم اصلاح دیتا، مدل به دقت ۷۰٪ محدود شد. مدل در این فاز دچار **Overfitting** شدید بود و نویزهای دیتاسیت را به جای الگوهای بیماری یاد می‌گرفت.

### فاز دوم: Regularization & Control (دقت ۸۵٪)
* **استراتژی:** اضافه کردن لایه‌های **Dropout**، استفاده از **EarlyStopping** و **ModelCheckpoint** برای توقف آموزش در بهترین نقطه.
* **نتیجه:** دقت مدل به **۸۵٪** ارتقا یافت و با عملکرد مدل اختصاصی ما برابر شد. این مرحله ثابت کرد که مدل‌های عمیق بدون مکانیزم‌های کنترلی، برای دیتاسیت‌های پزشکی مناسب نیستند.

### فاز سوم: Learning Rate Optimization (دقت ۸۴٪)
* **استراتژی:** اضافه کردن **ReduceLROnPlateau** و کاهش نرخ یادگیری اولیه (Learning Rate) برای رسیدن به دقت میکروسکوپی در وزن‌ها.
* **نتیجه:** دقت به **۸۴٪** رسید. این کاهش جزئی نشان داد که نرخ یادگیری بسیار پایین در این مسئله خاص، مدل را در **Local Minima** (بهینه‌های محلی) متوقف می‌کند.



---

## 🛠 تفسیرپذیری با Grad-CAM (Explainable AI)
برای اعتمادپذیری در کاربرد پزشکی، از تکنیک **Grad-CAM** استفاده شده است.
* **عملکرد:** این ابزار با محاسبه گرادیان‌های لایه کانولوشن نهایی، مناطق درگیر در ریه را به صورت نقشه حرارتی (Heatmap) روی تصویر اصلی نمایش می‌دهد.
* **مزیت:** این قابلیت به پزشک کمک می‌کند تا متوجه شود مدل دقیقاً روی کدام نواحی مشکوک تمرکز کرده است.



---

## 🐳 استقرار و دپلوی (Dockerization)
پروژه کاملاً داکرایز شده تا فرآیند اجرا در هر محیطی کاملاً پایدار و مستقل باشد.

### ویژگی‌های Dockerfile:
* **Base Image:** تصویر رسمی `tensorflow/tensorflow:latest`.
* **Preprocessing Sync:** اعمال دقیق `Samplewise Normalization` در کانتینر.
* **Port:** اجرا روی پورت `5005`.

### راهنمای اجرا:
1. **ساخت ایمیج:** `docker build -t pneumonia-app .`
2. **اجرای کانتینر:** `docker run -p 5005:5005 pneumonia-app`

---

## ⚙️ جزییات پیش‌پردازش (Preprocessing)
تمامی تصاویر ورودی در زمان Predict طبق پروتکل آموزش اصلاح می‌شوند:
* **Resizing:** تغییر ابعاد به $180 \times 180$.
* **Standardization:** اعمال میانگین صفر و انحراف معیار یک (`Samplewise`).
* **Data Type:** تبدیل به `float32`.

---

### 👤 نویسنده
**حامد** - Deep Learning Researcher
